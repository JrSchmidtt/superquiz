<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SUPER QUIZ - HOME</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <img src="/img/logo.svg" alt="Logo IPSUM">
    <H1>SUPER QUIZ</H1>
    <hr>
    <h3>Lista de perguntas</h3>
    <p>Nome: <%= nome %></p>
    <p>Dev: <%= lang %></p>

    <% if (msg == true) { %>
        <h3> Isso é uma mensagem de erro : <%= msg %> </h3>
    <%} else { %>
        <h3>Nenhum erro encontrado!</h3>
    <%}%>

    <% produtos.forEach(function(produto) { %>
        <p> Produto: <%= produto.nome %> Valor: <%= produto.preco %> </p>
    <% }) %>
</body>
</html>


